<?php
$this->placeholder('optionsHeadline')->set($this->getTranslation('Current Blogs'));
if ($this->blogs != null) {
    echo '<ul>';
    foreach ($this->blogs as $blog) {
        echo '<li>' . $this->link($blog->name, '/mod_blog/blog/edit/id/' . $blog->id, 'report.png') . '</li>';
    }
    echo '</ul>';
} else {
  echo '<p>' . $this->getTranslation('You do not have any blogs currently') . '</p>';
}
?>

